using UnityEngine;
// 敵の出す自機外し弾のスクリプト

public class EnemyUniqueOrbit : EnemyBullets
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    public override void Start()
    {
        base.Start();
    }
}
